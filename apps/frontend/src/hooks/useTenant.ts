// Stub — full implementation in Step 03
// Will inject CSS variables from tenant.theme_config into document.documentElement
export function useTenant() {
  return {
    tenant: null,
    themeConfig: null,
  }
}
