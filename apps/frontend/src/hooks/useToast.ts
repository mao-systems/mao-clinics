// Stub — toast notification system wired in Step 02 (UI components)
export function useToast() {
  return {
    toast: (_message: string, _type?: 'success' | 'error' | 'info' | 'warning') => {
      // Will call toast library in Step 02
    },
  }
}
